# URL of your backend proxy that forwards requests to Anthropic.
# Leave unset locally if you're running the proxy at /api/claude on the same origin.
VITE_CLAUDE_PROXY_URL=

# Used only by the example server/proxy.js — never exposed to the browser.
ANTHROPIC_API_KEY=
